{
  "name": "neary-gtfs",
  "version": "0.2.0-m1",
  "private": true,
  "type": "module",
  "description": "Multi-feed GTFS publisher for the neary v2 PWA (Transitous-aligned, with CTP Cluj enhancement)",
  "scripts": {
    "pipeline": "node src/pipeline/build-all.js",
    "build:cluj-napoca": "node feeds/cluj-napoca/build.js",
    "test": "vitest --run",
    "lint": "eslint src/ feeds/"
  },
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1",
    "archiver": "^7.0.1",
    "better-sqlite3": "^12.11.1",
    "csv-parse": "^7.0.0",
    "node-stream-zip": "^1.15.0"
  },
  "devDependencies": {
    "vitest": "^4.0.0"
  }
}
