name: Check Standards Drift

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  drift-check:
    uses: n3ary/actions/.github/workflows/check-standards-drift.yml@v1
    with:
      vendor-dir: docs/standards
