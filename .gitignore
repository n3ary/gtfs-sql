node_modules/
apps/*/outputs/
apps/*/dist/
libs/*/outputs/
libs/*/dist/
*.log
.env
.tsbuildinfo
*.tsbuildinfo

# macOS Finder metadata (created by desktop folder views)
.DS_Store

# Per-user state from local AI editor sessions — never committed.
.kiro/
.claude/
.kilo/worktrees/
# CI deploy bundle (Cloudflare Pages) — never committed
deploy/
