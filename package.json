{
  "name": "neary-gtfs",
  "version": "0.2.2-m1",
  "private": true,
  "type": "module",
  "description": "App-side GTFS → SQLite + feeds.json registry for the neary PWA",
  "scripts": {
    "pipeline": "node src/pipeline/build-all.js",
    "test": "vitest --run --passWithNoTests"
  },
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1",
    "archiver": "^7.0.1",
    "better-sqlite3": "^12.11.1",
    "csv-parse": "^7.0.1",
    "node-stream-zip": "^1.15.0"
  },
  "devDependencies": {
    "vitest": "^4.0.0"
  }
}
