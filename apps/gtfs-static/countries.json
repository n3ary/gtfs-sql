{
  "_comment": "Which Transitous country ro.json files we scan. The actual publish set is auto-derived from feeds/<id>/config.json directories: a feed is published iff its config's `enhances` value matches a Transitous source name in the scanned countries. See resolve-feeds.ts for the merge logic.",
  "countries": ["ro"]
}
