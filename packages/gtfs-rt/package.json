{
  "name": "@gtfs/rt",
  "version": "0.0.0",
  "private": true,
  "description": "Live GTFS-RT adapter — polls upstream feeds, applies per-feed quirks, publishes a clean FeedMessage. Placeholder until issue #34 step 7 populates it.",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "echo 'no tests yet' && exit 0",
    "lint": "echo 'no lint yet' && exit 0"
  },
  "devDependencies": {
    "typescript": "^6.0.0"
  }
}