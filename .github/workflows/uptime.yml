name: gtfs-rt uptime

# Probes gtfs-rt.n3ary.com every 15 minutes. Tracks failure state
# via the GH Actions run history (no external state store needed).
# On 3 consecutive failures, opens a single GH issue (idempotent -
# re-uses the same issue if one is already open). Labels the issue
# `vm-restart-pending` to trigger the recovery workflow, which
# reboots the VM via the Hetzner API. The systemd unit
# (`neary-gtfs-rt.service`) is enabled, so the container auto-
# starts on boot and the next probe flips back to healthy.
#
# What this catches: service down, container crash loop, network
# partition to the VM, Hetzner infra outage. The on-VM systemd
# timer (PR #154 era) is the first-line recovery; this monitor
# is the second-line that catches the cases the timer doesn't
# (e.g. the unit itself is masked, or systemd is wedged).
#
# What this does NOT catch: silent data corruption (the service
# returns 200 but vehicle_positions has 0 entities). The
# /rt/cluj-napoca/vehicle_positions probe is a partial
# defense (asserts the response body is non-empty), but a
# thorough check would need an entity count assertion. Cheap
# enough to add if you've seen that mode of failure.
#
# State machine:
#   HEALTHY  -> all last 3 cron runs are success; no issue open
#   DEGRADED -> one or two of the last 3 runs are failure
#   DOWN     -> all last 3 runs are failure; issue open with
#               `vm-restart-pending` label; recovery workflow
#               has been triggered (or is running)
#   recovery -> DEGRADED -> HEALTHY once 1+ success after DOWN
#
# This is `continue-on-error` on the probe step so a single bad
# run doesn't kill the cron. State is determined AFTER the probe.

on:
  schedule:
    - cron: '*/15 * * * *'
  workflow_dispatch:
    inputs:
      simulate_failures:
        description: 'Skip the real probe and treat this run as a failure (for testing the issue-open path).'
        required: false
        type: boolean
        default: false
      reset_state:
        description: 'Close any open uptime issue and exit (for testing cleanup).'
        required: false
        type: boolean
        default: false
      recover_now:
        description: 'Manually trigger the recovery workflow (reboot the VM). Use only after you understand the impact.'
        required: false
        type: boolean
        default: false

permissions:
  contents: read
  issues: write
  actions: read

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 4
    steps:
      - name: Check out the repo
        # Required: `gh issue create` runs `git` internally for branch
        # context and errors out with "not a git repository" without
        # this. Default `actions: read` is enough for `gh api` to read
        # run history.
        #
        # Pinned to v7 (not v4) because v4 targets Node 20, which is
        # deprecated on GitHub Actions runners as of 2025-09-19 and
        # gets force-promoted to Node 24 with a deprecation warning.
        # v5+ is rebuilt for Node 24; v7 matches the rest of the
        # workflows in this repo (build-gtfs-rt, daily, pr-validation,
        # publish-spec).
        uses: actions/checkout@v7

      - name: Ensure incident labels exist
        # `gh issue create --label X` fails with "label not found"
        # if X doesn't exist. Create the label on every run
        # (idempotent; `gh label create` exits non-zero if it
        # already exists, which we swallow). Cheap - one API call.
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -uo pipefail
          gh label create "vm-restart-pending" \
            --description "VM reboot pending - uptime monitor detected 3 consecutive failures" \
            --color "D93F0B" 2>/dev/null || true

      - name: Reset state (workflow_dispatch only)
        if: ${{ inputs.reset_state == 'true' }}
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          ISSUE=$(gh issue list --state open --search "gtfs-rt.n3ary.com DOWN in:title" --json number --jq '.[0].number // empty')
          if [ -n "$ISSUE" ]; then
            gh issue close "$ISSUE" --comment "Closed by manual reset (workflow_dispatch reset_state=true)."
            echo "closed issue #$ISSUE"
          else
            echo "no open issue to close"
          fi

      - name: Skip rest if reset
        if: ${{ inputs.reset_state == 'true' }}
        run: |
          echo "reset done - exiting"
          exit 0

      - name: Probe /healthz and /api/rt/cluj-napoca/vehicle_positions
        id: probe
        if: ${{ inputs.simulate_failures != 'true' && inputs.reset_state != 'true' }}
        env:
          SIMULATE_FAILURES: ${{ inputs.simulate_failures }}
        run: |
          set -uo pipefail
          # SIMULATE_FAILURES: when this run is meant to be a
          # failure (for testing), skip the curl calls. The
          # success/failure of the step is what matters for the
          # state machine below; we don't need to surface the
          # curl error in the run summary.
          if [ "${SIMULATE_FAILURES:-false}" = "true" ]; then
            echo "::notice::simulate_failures=true; skipping real probe, treating as failure"
            echo "ok=0" >> "$GITHUB_OUTPUT"
            exit 1
          fi
          HSTATUS=$(curl -sS -m 10 -o /tmp/h.body -w '%{http_code}' https://gtfs-rt.n3ary.com/healthz || true)
          HFAIL=$?
          echo "healthz status=$HSTATUS curl_exit=$HFAIL"
          H_BODY=$(head -c 200 /tmp/h.body 2>/dev/null || echo "")
          echo "healthz_body=$H_BODY"
          VSTATUS=$(curl -sS -m 10 -o /tmp/v.body -w '%{http_code}' https://gtfs-rt.n3ary.com/rt/cluj-napoca/vehicle_positions || true)
          VFAIL=$?
          echo "vehicle_positions status=$VSTATUS curl_exit=$VFAIL"
          V_BODY=$(head -c 200 /tmp/v.body 2>/dev/null || echo "")
          echo "vehicle_positions_body=$V_BODY"
          # Both endpoints must return 200 AND the body must be
          # non-empty. The 200 alone isn't enough - a misbehaving
          # service can return 200 with an empty body.
          if [ "$HSTATUS" != "200" ] || [ "$VSTATUS" != "200" ]; then
            echo "::error::one or both endpoints non-200"
            echo "ok=0" >> "$GITHUB_OUTPUT"
            exit 1
          fi
          if [ ! -s /tmp/h.body ] || [ ! -s /tmp/v.body ]; then
            echo "::error::one or both bodies empty"
            echo "ok=0" >> "$GITHUB_OUTPUT"
            exit 1
          fi
          if ! grep -q '"entity"' /tmp/v.body; then
            echo "::error::vehicle_positions body missing 'entity' (silent-data-corruption sentinel)"
            echo "ok=0" >> "$GITHUB_OUTPUT"
            exit 1
          fi
          echo "ok=1" >> "$GITHUB_OUTPUT"

      - name: Skip rest if simulating recovery
        if: ${{ inputs.recover_now == 'true' }}
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          echo "manually triggering recovery workflow"
          gh workflow run recovery.yml \
            --repo '${{ github.repository }}' \
            -f reason="manual-trigger-from-uptime-cron"
          echo "recovery workflow dispatched"

      - name: Compute state from last 3 runs
        if: ${{ always() }}  # run even when the probe failed - the
                              # whole point of the state machine is to
                              # detect failed runs
        id: state
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SIMULATE_FAILURES: ${{ inputs.simulate_failures }}
          PROBE_OK: ${{ steps.probe.outputs.ok }}
        run: |
          set -euo pipefail
          # Decide the CURRENT run's conclusion from the inputs +
          # probe step output. The API would tell us, but the
          # current run is still in progress at this point and its
          # conclusion field is null. So the API gives us only the
          # 2 prior runs reliably; we need to fill in the 3rd slot
          # ourselves.
          if [ "${SIMULATE_FAILURES:-false}" = "true" ]; then
            CURRENT="failure"
            CURRENT_REASON="simulate_failures=true"
          elif [ -z "${PROBE_OK:-}" ]; then
            # Probe was skipped (only happens if reset_state=true, but
            # reset_state short-circuits the whole job before this).
            # Treat as failure to be safe - we don't want to silently
            # miss a real outage.
            CURRENT="failure"
            CURRENT_REASON="probe skipped"
          elif [ "$PROBE_OK" = "0" ]; then
            CURRENT="failure"
            CURRENT_REASON="probe returned ok=0"
          else
            CURRENT="success"
            CURRENT_REASON="probe returned ok=1"
          fi
          # Fetch the 2 PRIOR runs. (The current one isn't done yet
          # and would have a null conclusion, which would throw off
          # the count. Drop the first entry of the response, which
          # is the current in-progress run.)
          PRIOR=$(gh api "repos/${{ github.repository }}/actions/workflows/uptime.yml/runs?per_page=3" \
            --jq '.workflow_runs | .[1:] | map(.conclusion) | join(",")')
          CONCLUSIONS="$PRIOR,$CURRENT"
          echo "last 3 conclusions: $CONCLUSIONS (current=$CURRENT reason=$CURRENT_REASON)"
          FAIL_COUNT=$(echo "$CONCLUSIONS" | tr ',' '\n' | grep -c '^failure$' || true)
          echo "fail_count=$FAIL_COUNT" >> "$GITHUB_OUTPUT"
          # Surface the conclusions string for downstream steps.
          # Each step runs in a fresh shell, so a bare
          # `$CONCLUSIONS` here is NOT visible in the next
          # step's bash. We have to ferry it through the
          # GITHUB_OUTPUT protocol. Body-of-issue below reads
          # this via ${{ steps.state.outputs.conclusions }}.
          echo "conclusions=${CONCLUSIONS}" >> "$GITHUB_OUTPUT"
          if [ "$FAIL_COUNT" -ge 3 ]; then
            echo "state=DOWN" >> "$GITHUB_OUTPUT"
          elif [ "$FAIL_COUNT" -ge 1 ]; then
            echo "state=DEGRADED" >> "$GITHUB_OUTPUT"
          else
            echo "state=HEALTHY" >> "$GITHUB_OUTPUT"
          fi

      - name: Open or close the incident issue
        if: ${{ always() && inputs.reset_state != 'true' && inputs.recover_now != 'true' }}
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          STATE: ${{ steps.state.outputs.state }}
          CONCLUSIONS: ${{ steps.state.outputs.conclusions }}
        run: |
          set -euo pipefail
          ISSUE=$(gh issue list --state open --search "gtfs-rt.n3ary.com DOWN in:title" --json number --jq '.[0].number // empty')
          echo "current open issue: $ISSUE"
          if [ "$STATE" = "DOWN" ]; then
            if [ -z "$ISSUE" ]; then
              echo "creating incident issue"
              # `gh issue create` does NOT support --json. It
              # prints the issue URL to stdout on success; we
              # extract the issue number from the trailing path
              # segment.
              ISSUE_URL=$(gh issue create \
                --title "gtfs-rt.n3ary.com DOWN — $(date -u +%FT%TZ)" \
                --label "vm-restart-pending" \
                --body "$(cat <<EOF
          \`gtfs-rt.n3ary.com\` has failed 3 consecutive uptime
          checks. The recovery workflow has been triggered
          (Hetzner server reboot; the systemd unit
          \`neary-gtfs-rt.service\` auto-starts the container on
          boot).

          - Last 3 conclusions: ${CONCLUSIONS}
          - Triggered at: $(date -u +%FT%TZ)
          - First check after this issue was opened will close
            it if the service is back. The issue is reopened
            on the next 3-failure cycle.
          EOF
          )")
              ISSUE=$(echo "$ISSUE_URL" | grep -oE '/issues/[0-9]+$' | grep -oE '[0-9]+$')
              echo "created issue #$ISSUE ($ISSUE_URL)"
            else
              echo "issue already open: #$ISSUE - ensuring label"
              gh issue edit "$ISSUE" --add-label "vm-restart-pending" || true
            fi
            # Trigger the recovery workflow. Use the issue as a
            # natural idempotency token: only one open issue means
            # only one recovery at a time. The recovery workflow
            # itself checks for the label.
            #
            # The trigger path is `recovery.yml`'s
            # `issues: types: [opened, labeled]` event - the
            # `gh workflow run` below is documented but always fails
            # with HTTP 403 because GITHUB_TOKEN cannot be used to
            # trigger workflows (this is a GHA security restriction,
            # not a permission config issue). The `issues.opened`
            # event fires reliably when `gh issue create` runs.
            echo "triggering recovery workflow (manual dispatch)"
            gh workflow run recovery.yml \
              --repo '${{ github.repository }}' \
              -f reason="uptime-cron-3-consecutive-failures" \
              || echo "warn: gh workflow run failed; the issues.opened/labeled event should still fire"
          else
            # HEALTHY or DEGRADED. If there's an open issue, close
            # it - the service is back (or the failures are
            # intermittent and the issue is no longer accurate).
            if [ -n "$ISSUE" ]; then
              echo "closing issue #$ISSUE - state=$STATE"
              gh issue close "$ISSUE" --comment "Auto-closed: state=$STATE (most recent cron run succeeded)."
            else
              echo "no incident issue to close"
            fi
          fi
          echo "final_state=$STATE" >> "$GITHUB_OUTPUT"
